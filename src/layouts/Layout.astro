---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="tr" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>

		<!-- GOOGLE FONTS -->
		<link rel="preconnect" href="https://googleapis.com">
		<link rel="preconnect" href="https://gstatic.com" crossorigin>
		<link href="https://googleapis.com/css2?family=Archivo:wght@100;200;300;400&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">
	</head>
	<body class="bg-[#FAF9F6] text-[#111111] antialiased selection:bg-[#111111] selection:text-white">
		<slot />
	</body>
</html>

<style is:global>
	/* Tailwind v4 Ana Entegrasyonu */
	@import "tailwindcss";

	body {
		font-family: 'Inter', sans-serif;
	}
	
	h1, h2, h3, .font-mono-arch {
		font-family: 'Archivo', sans-serif;
	}

	img {
		content-visibility: auto;
	}

	.gallery-frame {
		opacity: 0;
	}

	.gallery-frame.is-active {
		opacity: 1;
	}

	#lightbox {
		display: none;
	}

	#lightbox.is-open {
		display: flex;
	}
</style>